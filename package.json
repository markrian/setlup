{
  "name": "setlup",
  "version": "0.0.0",
  "description": "don't know yet",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && browserify -r ./src/setlup.js:setlup -t [ babelify --presets [ es2015 ] ] -o ./build/index.js",
    "test": "istanbul cover _mocha -- --compilers js:babel/register test/unit",
    "tdd": "mocha --watch --debug --reporter min --compilers js:babel/register test/unit"
  },
  "author": "Mark Florian <markrian@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "chai": "^3.4.1",
    "chance": "~0.8.0",
    "istanbul": "^0.4.1",
    "mocha": "^2.2.5"
  },
  "dependencies": {
    "big-rational": "^0.10.0",
    "lodash": "^3.10.1"
  }
}
